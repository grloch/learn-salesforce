<header>

    <div class="container">
        <div class="row">
            <div class="col">
                <span class="h3">Learn Salesforce</span>
                {% if pageTitle %}
                    <h1>{{pageTitle}}</h1>
                {% endif %}
            </div>
        </div>
    </div>

</header>